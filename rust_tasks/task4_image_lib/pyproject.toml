[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "image_resize"
requires-python = ">=3.8"
version = "0.1.0"
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]
